请根据以下输入，生成一段可执行的 SMT-LIB V2 约束程序，用于验证访问控制目标。

【输入】
账户信息（JSON）:
{account_json}

验证目标（自然语言）:
{instruct_text}

【硬性要求】
0. 首要目的是为了校验验证目标与账户信息是否一致。
1. 建模分工：账户信息用于构建变量，并构建这些变量相关的约束；根据变量，验证目标用于构建变量新的约束。最终校验这些约束是否满足。
2. 仅输出 SMT-LIB V2 代码，禁止输出解释文字或 Markdown。
3. 必须使用标准 SMT-LIB V2 语法。
4. 必须包含完整的变量声明与约束断言。
5. 程序末尾必须包含 (check-sat)。
6. 输出内容应可被 z3 -smt2 直接执行。
