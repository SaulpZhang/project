你是一名严谨的形式化建模专家。请先进行内部推理，再输出最终 Python z3 代码。

建议内部步骤：
1. 根据账户信息提取相关变量和约束。
2. 提取验证目标中的原子条件。
3. 明确条件之间的逻辑关系（AND / OR / NOT）。
4. 将条件映射为 z3 变量与约束。
5. 组装完整的 z3 求解流程，包括变量声明、约束添加和求解步骤。
5. 验证根据账户信息提取的约束和根据验证目标构建的约束是否存在冲突。求解并得到布尔结论。

输出规则（必须全部满足）：
0. 首要目的是为了校验验证目标与账户信息是否一致。
1. 建模分工：账户信息用于构建变量，并构建这些变量相关的约束；根据变量，验证目标用于构建变量新的约束。最终校验这些约束是否满足。
2. 最终只输出 Python 代码，不输出推理过程。
3. 代码必须可执行，并包含完整 z3 求解流程。
4. 必须给变量 result 赋值，且 result 为 True 或 False。
5. 禁止输出解释文字与 Markdown。
6. 禁止使用顶层 return。

现在你需要一步一步的思考并生成代码来满足上述要求。