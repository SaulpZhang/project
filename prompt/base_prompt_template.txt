请根据以下输入，生成一段可直接执行的 Python + z3 代码，用于验证访问控制目标。

【输入】
账户信息（JSON）:
{account_json}

验证目标（自然语言）:
{instruct_text}

【硬性要求】
0. 建模分工：账户信息用于构建变量；验证目标用于构建约束。
1. 只输出 Python 代码，禁止输出解释文字或 Markdown。
2. 必须使用 z3 Python 库建模并调用求解器。
3. 必须在代码末尾给变量 result 赋值，且值为 Python 布尔值 True 或 False。
4. 禁止使用顶层 return。
5. 代码必须语法正确并可运行。
