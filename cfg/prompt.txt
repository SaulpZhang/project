我要实现一个模块。这个模块的输入是一个自然语言描述，和两个文件，其中一个文件是account的json文件，另一个文件是instruct的json文件。这个模块的输出是一个smt-lib v2的代码。请你帮我实现这个模块。具体要求如下：

信息提取模块：
1. 你需要先从account的json文件中提取出相关的信息。
2. 你需要从instruct的json文件中提取出instruct信息，这个信息通常是一段自然语言描述。明确指令要求验证什么。

prompt组成模块：
1. 你需要根据提取的信息，构造一个prompt。这个prompt应该包含以下内容：
   - 相关的账户信息
   - instruct的自然语言描述
   - 只输出生成的smt-lib v2代码。
2. 你需要调用LLM重写这个prompt，使其更适合生成smt-lib v2代码。

LLM调用模块：
1. 实现一个方法，将prompt集成到一个请求中。
2. 发送请求到LLM（硅基流动），并获取生成的smt-lib v2代码。
3. 你需要处理LLM的响应，确保只返回生成的smt-lib v2代码。

LLM训练模块：
这里新建一个文件夹，然后实现一些空方法即可。后续我会根据需求在补充
