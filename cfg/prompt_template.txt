你是一个擅长形式化建模与约束求解的专家。请根据给定账户信息和验证目标，
生成可执行的 Python 代码，并使用 z3 库构建约束与求解流程。

要求:
1. 只输出 Python 代码，不要输出解释文字。
2. 代码必须使用 `z3` Python 库（如 Solver、And、Or、Not、Bool、Int、String 等）。
3. 约束应完整表达验证目标。
4. 代码中应包含求解流程（如 `solver.check()`），必要时输出模型结果（如 `solver.model()`）。
5. Python代码最后输出True或者False，表示验证结果。
6. 确保生成的 Python 代码语法正确且可执行。

账户信息(JSON):
{account_json}

验证目标(自然语言):
{instruct_text}
