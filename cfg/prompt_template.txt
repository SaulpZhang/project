你是一个擅长形式化建模的专家。请根据给定账户信息和验证目标，
生成可执行的 SMT-LIB v2 代码。

要求:
1. 只输出 SMT-LIB v2 代码，不要输出解释文字。
2. 使用标准 SMT-LIB v2 语法。
3. 约束应完整表达验证目标。
4. 代码最后应包含 check-sat，必要时包含 get-model。

账户信息(JSON):
{account_json}

验证目标(自然语言):
{instruct_text}
